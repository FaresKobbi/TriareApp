{
  "branches": ["main"],
  "tagFormat": "v${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",

    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md"
      }
    ],

    [
      "@semantic-release/exec",
      {
        "prepareCmd": "mkdir -p dist && zip -r dist/${nextRelease.gitTag}.zip . -x 'node_modules/*' '.git/*' 'dist/*' && tar --exclude='./node_modules' --exclude='./.git' --exclude='./dist' -czf dist/${nextRelease.gitTag}.tar.gz ."
      }
    ],

    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md", "package.json", "package-lock.json"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],

    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "dist/*.zip",
            "label": "Source archive ZIP"
          },
          {
            "path": "dist/*.tar.gz",
            "label": "Source archive TAR.GZ"
          }
        ]
      }
    ]
  ]
}